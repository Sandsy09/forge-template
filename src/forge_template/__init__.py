"""forge-template's own tooling.

Not part of the scaffold — this package holds checks that validate
``copier.yml`` and ``template/`` against the invariants documented in
``CLAUDE.md``. Nothing here is copied into generated projects (see
``_exclude`` in ``copier.yml``, which excludes ``copier.yml`` itself; this
package lives entirely outside ``template/``).
"""
