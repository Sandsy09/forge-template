## Scientific Python

This project includes NumPy, pandas, Matplotlib, and scikit-learn as runtime
dependencies. Their public import names are `numpy`, `pandas`, `matplotlib`,
and `sklearn` respectively.

The generated `tests/test_scientific_python.py` smoke test verifies that the
complete stack remains importable in the locked project environment.
