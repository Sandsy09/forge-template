# Review routing. Every path falls to the maintainers team by default; add
# more specific rules above this line as the project grows.
* @{{ options.github.organisation }}/{{ project.repository_name }}-maintainers
