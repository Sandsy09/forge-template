# Copy to .env and fill in real values. .env is gitignored and is
# _skip_if_exists on `copier update`, so local values are never overwritten.
#
# This file is tracked and committed -- keep every entry a placeholder.
# Never fill in a real, plausible, or generated value here.
#
# EXAMPLE_API_KEY=
