# Normalise to LF in the repository. Without this, Windows checkouts get CRLF
# and `copier update` cannot match its baseline against the working tree.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.whl binary