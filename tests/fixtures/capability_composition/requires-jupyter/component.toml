# Synthetic capability for FT-11.04 / ADR 0052. NOT a production component --
# it exists only to exercise a code path the production catalogue cannot
# reach. See docs/capability-composition-validation.md.
#
# Rehearses the exact hard-dependency edge the future `data-science`
# archetype declares (`requires = [{ id = "jupyter", version = ">=1,<2" }]`),
# so FT-12.01 inherits a proven path.
manifest_version = 2
id = "requires-jupyter"
name = "Requires Jupyter"
description = "Synthetic capability with a hard dependency on jupyter."
kind = "capability"
version = "1.0.0"
content_root = "content"
requires = [{ id = "jupyter", version = ">=1,<2" }]
conflicts = []

[compatibility]
projectspec_protocols = [1]
requires_python = ">=3.11"
