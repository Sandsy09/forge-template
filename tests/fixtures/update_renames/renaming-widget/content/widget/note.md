# renaming-widget fixture

Synthetic FT-17.04 capability. Keeps `content_root` non-empty. The `from`/`to`
paths declared in `component.toml`'s `[[renames]]` records are metadata only
and do not need to name a real file here.
