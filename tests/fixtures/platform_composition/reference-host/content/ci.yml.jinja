name: CI
"on": [push]
env:
  ORG: {{ options.reference_host.organisation }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
      [[forge:extension ci-steps]]
  [[forge:extension ci-jobs]]
