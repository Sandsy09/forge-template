# CF-16.01 — Define engine-default selection and source-resolution UX

## Outcome

Settle defaults, configuration precedence, explicit legacy access, override
security and flag deprecations.

## Issue context

The released baseline is create-forge 0.3.2 with default direct-Copier
generation and an optional, hidden engine preview over forge-template
>=0.4.1,<0.5. Preview generation does not provide engine-native updates. This
draft describes future work, not a shipped interface.

Provider manifests, composition, generated content and in-memory validation
stay in forge-template. Client UX, ProjectSpec construction, staging,
filesystem application, Git/hooks and error presentation stay in create-forge.

## Problem statement

Settle defaults, configuration precedence, explicit legacy access, override
security and flag deprecations. The current release does not establish
completion of this requirement; it needs the evidence and ownership checks
below.

## Proposed resolution

- Review predecessor decisions and current living contracts, compare
  alternatives and obtain explicit maintainer approval for each unresolved
  design choice in the acceptance checklist.
- Record the accepted choice in a new ADR and update its owning living
  contract; preserve historical ADRs and completed roadmap evidence.
- Reconcile affected implementation drafts, acceptance tests and dependency
  edges before unblocking them. If the contract reveals larger work, split it
  into bounded issues before implementation.

## Acceptance criteria

- [ ] Define ordinary installation, default path, list/doctor behaviour,
  generic component selection, answer precedence, interactive cancellation and
  non-interactive requirements.
- [ ] Reconcile the atomic --template-url replacement described by the
  existing engine-resolution contract/ADR 0011 with explicit legacy access;
  define preview and old-flag deprecation sequencing in a new ADR.
- [ ] Define --engine-source/--engine-ref isolation, source validation,
  warnings under --yes, supported bounds and no-fallback errors; ordinary
  answer configuration must not redirect executable sources.

## Review obligations

These review criteria apply to this issue's assigned slice; other listed
owners complete the remaining slices. Decision-issue exclusions apply to the
decision itself, not to the entire delivery roadmap.

### CF-ROADMAP-01-AC-01

A living CLI contract defines the default, explicit legacy route,
configuration precedence, prompts, `--yes` requirements and deprecation
timeline.

Owners: [CF-16.01](https://github.com/Sandsy09/create-forge/issues/155).

### CF-ROADMAP-01-AC-02

Data still flows `create-forge → ProjectSpec → forge-template render →
create-forge finalisation`; no component semantics or resources are copied
downstream.

Owners: [CF-16.01](https://github.com/Sandsy09/create-forge/issues/155), [CF-18.01](https://github.com/Sandsy09/create-forge/issues/158).

### CF-ROADMAP-01-AC-04

Missing/incompatible providers, invalid selections and render/finalise
failures have stable statuses and never silently fall back to Copier.

Owners: [CF-16.01](https://github.com/Sandsy09/create-forge/issues/155), [CF-18.06](https://github.com/Sandsy09/create-forge/issues/163).

### CF-ROADMAP-01-EX-01

No provider manifest, composition or generated-content changes.

Owners: [CF-16.01](https://github.com/Sandsy09/create-forge/issues/155).

### CF-ROADMAP-01-EX-02

No new archetype.

Owners: [CF-16.01](https://github.com/Sandsy09/create-forge/issues/155).

### CF-ROADMAP-01-EX-03

No remote plugin/registry system.

Owners: [CF-16.01](https://github.com/Sandsy09/create-forge/issues/155).

## Exclusions

- No runtime implementation, generated-content change, protocol increment,
  package version bump or release in this decision issue.
- Do not preselect metadata filenames, merge algorithms, Streamlit layout or
  deprecation dates without the accepted decision.
- No arbitrary remote component registry or plugin execution; no new shared
  Forge runtime dependency for generated projects.

## Dependencies

- Blocked by [FT-15.04](https://github.com/Sandsy09/forge-template/issues/149).

Acceptance of the predecessor contract, not a future provider release, is
the design gate.

## Parent epic

[CF-EPIC-16](https://github.com/Sandsy09/create-forge/issues/152)

## Milestone

Engine-Default Client Contracts — Stage 16

## Roadmap

[Stage 16](https://github.com/Sandsy09/create-forge/blob/main/docs/roadmap-v3/roadmap/16-engine-default-client-contracts/README.md)

## Labels

`area:cli`, `area:copier`, `area:registry`, `area:packaging`, `area:docs`,
`area:tests`, `type:decision`, `size:m`, `priority:medium`, `roadmap:16`,
`cross-repo`, `status:needs-decision`, `status:blocked`

## Required completion evidence

- Accepted ADR and living-contract links, recorded maintainer decisions and
  an approved acceptance matrix.
- Updated downstream draft/dependency links proving no implementation
  requirement is left implicit.
- Documentation validation and reviewed PR evidence under the owning
  repository's CONTRIBUTING workflow.
