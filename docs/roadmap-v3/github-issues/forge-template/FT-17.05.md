# FT-17.05 — Validate provider parity, reproducibility and distributions

## Outcome

Execute the approved matrix, downstream-facade checks and wheel/sdist audits.

## Issue context

The released baseline is create-forge 0.3.2 with default direct-Copier
generation and an optional, hidden engine preview over forge-template
>=0.4.1,<0.5. Preview generation does not provide engine-native updates. This
draft describes future work, not a shipped interface.

Provider manifests, composition, generated content and in-memory validation
stay in forge-template. Client UX, ProjectSpec construction, staging,
filesystem application, Git/hooks and error presentation stay in create-forge.

## Problem statement

Execute the approved matrix, downstream-facade checks and wheel/sdist audits.
The current release does not establish completion of this requirement; it
needs the evidence and ownership checks below.

## Proposed resolution

- Read the accepted predecessor contracts and implement only this issue's
  assigned outcome through the owning repository boundary.
- Add executable evidence for each acceptance criterion and update affected
  living documentation. Stop for a contract decision if required behaviour is
  still unresolved.

## Acceptance criteria

- [ ] Execute every provider acceptance row against the candidate and record
  commands, platforms, versions and artefact identities.
- [ ] Verify repeated rendering, supported update histories, existing
  archetype output and independent downstream public-facade consumption.
- [ ] Audit wheel/sdist resources and generated build/install/check
  behaviour; preserve supported direct-Copier generation/update regressions.

## Review obligations

These review criteria apply to this issue's assigned slice; other listed
owners complete the remaining slices. Decision-issue exclusions apply to the
decision itself, not to the entire delivery roadmap.

### FT-ROADMAP-01-AC-04

Failure and compatibility contracts preserve validation-before-render and do
not require `create-forge` to inspect component resources.

Owners: [FT-15.04](FT-15.04.md), [FT-17.05](FT-17.05.md).

### FT-ROADMAP-01-AC-06

Follow-up provider issues cover implementation, regression validation and
publication separately.

Owners: [FT-17.01](FT-17.01.md), [FT-17.05](FT-17.05.md),
[FT-17.06](FT-17.06.md).

## Exclusions

- No CLI flags/prompts, client configuration, Git/hook execution or
  destination finalisation in the provider.
- No package publication in this issue; its separate release child owns
  publication.
- No arbitrary remote component registry or plugin execution; no new shared
  Forge runtime dependency for generated projects.

## Dependencies

- Blocked by [FT-17.03](FT-17.03.md).
- Blocked by [FT-17.04](FT-17.04.md).

Provider adoption must use a reviewed immutable release, never a moving
branch. Local candidate pairing is validation evidence, not publication.

## Parent epic

[FT-EPIC-17](FT-EPIC-17.md)

## Milestone

Engine-Default Provider Implementation and Release — Stage 17

## Roadmap

[Stage
17](../../roadmap/17-engine-default-provider-implementation-and-release/README.md)

## Labels

`area:template`, `area:packaging`, `area:tests`, `type:test`, `size:l`,
`priority:medium`, `roadmap:17`, `cross-repo`, `status:blocked`

## Required completion evidence

- Merged PR and named regression commands/results against exact commits and
  dependency versions.
- Applicable fast checks, installed hooks, generated-project checks and
  protected CI evidence required by CONTRIBUTING.
- Updated living documentation and acceptance evidence; no success claim
  based solely on local-green tests.
